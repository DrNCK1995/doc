# Example screenshots

Capture these after `npm run dev` for documentation or App Store / clinic onboarding packs:

1. **Portfolio home** (`/`) — hero + Growth Monitoring CTA card
2. **Growth home** (`/growth`) — search + Register / Find CTAs
3. **Registration** (`/growth/register`) — form with live age Y/M/D
4. **Patient dashboard** — summary, Z-scores, nutrition badge, QR
5. **Charts tab** — WHO/IAP percentile curves + visit points
6. **PDF export** — downloaded clinical report

Tip: use the browser print dialog on `/growth/patients/[id]/print` for a clean chart printout.
